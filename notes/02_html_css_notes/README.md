# 🌐 HTML & CSS Notes Index

Welcome to the **HTML5 & CSS3** learning section (Month 3).

## 📋 Topics Outline
1. **HTML5 Semantic Tags**: `<header>`, `<nav>`, `<main>`, `<section>`, `<article>`, `<footer>`
2. **Text, Media & Forms**: Headings, paragraphs, anchors, images, input fields, and forms
3. **CSS Core & Box Model**: Selectors, colors, fonts, margins, borders, paddings
4. **Modern Layouts**: Flexbox (`display: flex`) & CSS Grid (`display: grid`)
5. **Responsive Design**: Media queries (`@media`), fluid typography, and viewport units
